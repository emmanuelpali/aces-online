Users register with email and password.
Every new user starts as a student.
An administrator grants instructor access.
Authentication uses short-lived access tokens and rotating refresh tokens.
Logout revokes the current refresh token.
Email verification and password reset are valuable, but they come after the core flow.
Social login and multi-factor authentication are postponed.